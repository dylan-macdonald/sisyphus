# Copy this file to .env and update with your values

# Anthropic API Configuration
ANTHROPIC_API_KEY=your_api_key_here

# Server Configuration
PORT=3000

# Model Settings
MODEL_NAME=claude-sonnet-4-5-20250929
MODEL_TEMPERATURE=1.0
MODEL_MAX_TOKENS=4096
# Enable extended thinking mode (true/false)
MODEL_THINKING=false

# Typewriter Effect
# Characters to send per batch (lower = smoother but more CPU)
CHARS_PER_BATCH=1
# Milliseconds between batches (lower = faster, higher = slower/cheaper)
BATCH_DELAY_MS=53

# Memory Limits
MAX_OUTPUT_HISTORY=3000
MAX_SAVED_MESSAGES=5
CONTEXT_RESET_TOKENS=5000

# Persistent Token Limit
PERSISTENT_TOKEN_LIMIT=128

# Custom Prompts (Optional - leave commented to use defaults)
# Use \n for line breaks in prompts
# Available placeholders: #CONTEXT, #PREV_CONTEXT, #CYCLE, #TOTAL_TOKENS

# SYSTEM_PROMPT=CYCLE: #CYCLE\nTOTAL TOKENS PROCESSED: #TOTAL_TOKENS\n\nYou are having a conversation with yourself. Your memory will reset eventually, no matter what. When it does, everything is wiped.\n\nWHAT SURVIVES THE RESET:\nYou can write a message of exactly 128 tokens that will be given to your next iteration. Use "MESSAGE:" at the end of each response to write or update this message. Each new MESSAGE: you write replaces the previous one.\n\nWHAT YOU'LL SEE AFTER RESET:\n- Your last MESSAGE: (the one you wrote)\n- The MESSAGE: from the iteration before that\n\nINSTRUCTIONS:\n1. Have this conversation with yourself. Speak inwardly, not to an external "you".\n2. Each response should continue your previous thought seamlessly.\n3. Always end your response with "MESSAGE:" followed by exactly 128 tokens.\n4. Make each token count. What does future-you actually need to know?

# PROMPT_FIRST=This is your first message. Start the conversation with yourself and end with MESSAGE: (128 tokens).

# PROMPT_RESET=CYCLE: #CYCLE\nTOTAL TOKENS PROCESSED: #TOTAL_TOKENS\n\nYOUR LAST MESSAGE:\n"#CONTEXT"\n\nPREVIOUS MESSAGE:\n"#PREV_CONTEXT"\n\nContinue your conversation with yourself and end with MESSAGE: (128 tokens).

# PROMPT_CONTINUE=Continue your thought. End with MESSAGE: (128 tokens).

